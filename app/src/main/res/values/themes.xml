<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.TrackYourExpenses2" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your primary branding color -->
        <item name="colorPrimary">#4A90E2</item> <!-- Nice vibrant blue -->
        <item name="colorOnPrimary">#FFFFFF</item>

        <!-- Secondary for accents (e.g. buttons) -->
        <item name="colorSecondary">#26A69A</item> <!-- Calming teal -->
        <item name="colorOnSecondary">#FFFFFF</item>

        <!-- Optional overrides -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimary</item>
        <item name="android:navigationBarColor" tools:targetApi="l">?attr/colorSurface</item>

        <!-- Typography (optional) -->
        <item name="textAppearanceHeadline6">@style/TextAppearance.Material3.HeadlineMedium</item>
        <item name="textAppearanceBody1">@style/TextAppearance.Material3.BodyLarge</item>
    </style>
</resources>
